# Word/Character Error Rate computation
jiwer>=3.1.0,<4.0.0

# Number to words conversion
# num2words is required for multilingual normalization
num2words

# BLEU score computation
# sacrebleu[ja,ko] is required for Japanese and Korean BLEU score computation
sacrebleu[ja,ko]

# Audio file I/O for dataset preparation
soundfile

# torchcodec requires FFmpeg shared libraries (not installable via pip).
# Install via system package manager before running pip install:
#   Linux: sudo apt install ffmpeg
#   macOS: brew install ffmpeg
torchcodec

# Lightweight text normalization (EnglishTextNormalizer)
whisper-normalizer
